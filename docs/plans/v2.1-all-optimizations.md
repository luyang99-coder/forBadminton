# forBadminton v2.1 全量优化实现方案

> **For Hermes:** Use subagent-driven-development skill to implement this plan feature-by-feature.
> 三个特性独立，可并行推进。每个特性内部任务串行。

**Goal:** 一次性完成 v2.1 三大优化：消息通知、数据看板、首页重构

**Architecture:** 微信小程序原生框架 + 微信云开发。index.js 是单文件 Page 对象 (~3900行)，所有数据通过 `persistActivity()` / `loadLocalActivity()` 持久化到本地存储和云端。WXML 直接用 inline 模板。样式在 `pages/index/styles/` 分文件管理。

**Tech Stack:** 微信小程序原生 (WXML + WXSS + JS)、微信云开发 (cloudfunctions)、Canvas 2D

---

## 项目关键文件

| 文件 | 用途 |
|------|------|
| `pages/index/index.js` | 主页面逻辑，单 Page 对象 ~3900行 |
| `pages/index/index.wxml` | 主页面模板 ~567行 |
| `pages/index/styles/theme.wxss` | 主样式 ~1260行 |
| `pages/index/styles/base.wxss` | 基础样式 |
| `pages/index/styles/roster-drawer.wxss` | 名单抽屉样式 |
| `pages/index/modules/config.js` | 配置常量 |
| `pages/index/modules/player.js` | 球员相关工具函数 |
| `pages/index/modules/schedule.js` | 轮转排阵核心 |
| `pages/index/modules/cloud-sync.js` | 云同步工具 |
| `pages/index/modules/stats-util.js` | 统计工具 |
| `pages/index/modules/canvas-export.js` | Canvas导出分享图 |
| `cloudfunctions/activityService/` | 云函数服务 |
| `pages/stats/` | 统计分包 |
| `pages/history/` | 历史分包 |

## 数据模型（activity 对象关键字段）

```javascript
{
  id, title, date, venue, status,       // 基本
  participants: [],                      // 球员数组 [{id, name, gender, level, status, ...}]
  schedule: [],                          // 轮转排阵
  completedSchedule: [],                 // 已完成计分
  fees: { items: [], payments: {}, ... } // 费用 (v2.1 已完成)
  // 需新增:
  notifySettings: { enabled: true, tmplIds: [] }
  // 首页卡片需要: homeCards: [{type, data}]
}
```

---

## 功能一：🔔 消息通知

### 背景
微信小程序通过 `wx.requestSubscribeMessage` API 实现订阅消息推送。需要：
1. 在微信公众平台配置订阅消息模板，获取模板 ID
2. 前端调用 `wx.requestSubscribeMessage` 让用户授权
3. 在关键事件触发时通过云函数调用 `subscribeMessage.send` 发送

### Task 1.1: 在 app.json 中声明订阅消息权限

**Objective:** 添加 `requiredPrivateInfos` 声明，确保小程序有权限调用订阅消息 API

**Files:**
- Modify: `app.json`

**Step 1: 修改 app.json**

在 `app.json` 根对象中添加：
```json
"requiredPrivateInfos": ["chooseAddress", "getLocation"],
"permission": {
  "scope.subscribeMessage": {
    "desc": "用于接收活动报名确认、轮转生成和比分更新的通知"
  }
}
```

**Step 2: 验证**
- 确认 JSON 格式合法
```bash
cd /c/Users/Lenovo/Desktop/forBadminton/forBadminton && cat app.json | python -m json.tool > /dev/null && echo "JSON OK"
```

### Task 1.2: 创建通知配置模块

**Objective:** 集中管理通知模板 ID 和触发规则

**Files:**
- Create: `pages/index/modules/notify.js`

**Step 1: 创建模块文件**

```javascript
// pages/index/modules/notify.js

// 订阅消息模板 ID（需在微信公众平台配置后替换）
const NOTIFY_TMPL_IDS = {
  activityConfirm: '',     // 活动创建/报名确认
  scheduleReady: '',       // 轮转排阵生成
  scoreUpdate: '',         // 比分更新提醒
  activityReminder: ''     // 活动前一天提醒（备用）
}

// 从 activity 配置中读取通知设置
function getNotifySettings(activity) {
  return activity.notifySettings || { enabled: true, tmplIds: Object.values(NOTIFY_TMPL_IDS).filter(Boolean) }
}

// 请求用户订阅消息授权
function requestSubscribe(callback) {
  const tmplIds = Object.values(NOTIFY_TMPL_IDS).filter(Boolean)
  if (!tmplIds.length) {
    if (callback) callback({ errMsg: 'no templates configured' })
    return
  }
  wx.requestSubscribeMessage({
    tmplIds: tmplIds,
    success: (res) => {
      if (callback) callback(null, res)
    },
    fail: (err) => {
      if (callback) callback(err)
    }
  })
}

// 通过云函数发送订阅消息
function sendSubscribeMessage(cloudEnv, openid, tmplId, data, page) {
  wx.cloud.callFunction({
    name: 'subscribeMessage',
    data: {
      env: cloudEnv,
      openid: openid,
      tmplId: tmplId,
      data: data,
      page: page
    },
    success: () => {},
    fail: (err) => console.error('send subscribe msg failed', err)
  })
}

module.exports = {
  NOTIFY_TMPL_IDS,
  getNotifySettings,
  requestSubscribe,
  sendSubscribeMessage
}
```

### Task 1.3: 创建云函数 subscribeMessage

**Objective:** 云函数发送订阅消息

**Files:**
- Create: `cloudfunctions/subscribeMessage/index.js`
- Create: `cloudfunctions/subscribeMessage/package.json`
- Create: `cloudfunctions/subscribeMessage/config.json`

**Step 1: 创建 package.json**
```json
{
  "name": "subscribeMessage",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
```

**Step 2: 创建 config.json**
```json
{
  "permissions": {
    "openapi": ["subscribeMessage.send"]
  }
}
```

**Step 3: 创建 index.js**
```javascript
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })

exports.main = async (event) => {
  const { openid, tmplId, data, page } = event
  try {
    await cloud.openapi.subscribeMessage.send({
      touser: openid,
      templateId: tmplId,
      data: data,
      page: page || 'pages/index/index'
    })
    return { success: true }
  } catch (err) {
    console.error('subscribeMessage.send failed', err)
    return { success: false, errMsg: err.message }
  }
}
```

**Step 4: 部署云函数**
```bash
# 在微信开发者工具中右键 cloudfunctions/subscribeMessage → 上传并部署
```

### Task 1.4: 在活动创建时触发订阅授权提示

**Objective:** 管理员创建活动后/用户报名后，引导开启通知

**Files:**
- Modify: `pages/index/index.js`
- Modify: `pages/index/index.wxml`

**Step 1: 在 index.js 中引入 notify 模块并添加授权提示方法**

在文件顶部 require 区域添加：
```javascript
const { requestSubscribe } = require('./modules/notify')
```

在 Page 对象的方法区域添加：
```javascript
promptSubscribe() {
  wx.showModal({
    title: '开启通知',
    content: '开启消息通知后，你将在活动报名确认、轮转生成和比分更新时收到微信提醒。',
    confirmText: '去开启',
    cancelText: '暂不',
    success: (res) => {
      if (res.confirm) {
        requestSubscribe((err, result) => {
          if (err) {
            console.log('subscribe rejected', err)
          } else {
            wx.showToast({ title: '已开启', icon: 'success' })
          }
        })
      }
    }
  })
}
```

**Step 2: 在报名成功后调用**

在 `signup` 或 `handleSignup` 方法成功后添加：
```javascript
// 首次报名后提示开启通知
const hasPrompted = wx.getStorageSync('notifyPrompted')
if (!hasPrompted) {
  wx.setStorageSync('notifyPrompted', true)
  this.promptSubscribe()
}
```

### Task 1.5: 在关键事件触发通知

**Objective:** 轮转生成、活动状态变更时发送通知

**Files:**
- Modify: `pages/index/index.js`
- Modify: `pages/index/modules/notify.js`

**Step 1: 在 generateSchedule 成功后添加通知逻辑**

在 `generateSchedule` 方法的 schedule 赋值之后：
```javascript
// 发送「轮转已生成」通知给所有参与者
const notifyModule = require('./modules/notify')
const participants = this.data.participants
participants.forEach(p => {
  if (p.openid && p.subscribed) {
    notifyModule.sendSubscribeMessage(
      'cloud1-d3gt5f02n0ddacfb3',
      p.openid,
      notifyModule.NOTIFY_TMPL_IDS.scheduleReady,
      {
        thing1: { value: this.data.activityTitle },
        thing2: { value: this.data.courtCount + '片场 · ' + this.data.schedule.length + '局' },
        time3: { value: this.data.generatedAt },
        thing4: { value: '点击查看你的轮转排阵' }
      },
      'pages/index/index?activityId=' + this.data.activityId
    )
  }
})
```

**Step 2: 在活动状态变更时发送通知**

在 `setActivityStatus` 方法中添加通知触发逻辑。

**验证:** 由于模板 ID 需要在微信后台配置，此阶段先用 mock 验证代码逻辑正确。
```bash
cd /c/Users/Lenovo/Desktop/forBadminton/forBadminton && node --check pages/index/index.js && echo "SYNTAX OK"
node --check pages/index/modules/notify.js && echo "NOTIFY SYNTAX OK"
git add -A && git commit -m "feat(notify): 订阅消息通知 — 模块+云函数+触发逻辑"
```

---

## 功能二：📊 数据看板

### 背景
当前 stats 分包有基本统计（胜率、净胜分、最佳搭档、雷达图），但缺少：
1. 胜率趋势折线图
2. 搭档关系网络图
3. 能力变化五维雷达动画
4. 数据看板汇总页

方案：用 Canvas 2D 绘制图表，不引入第三方库（保持分包体积小）。

### Task 2.1: 创建绘制工具模块

**Objective:** Canvas 2D 图表绘制基础工具

**Files:**
- Create: `pages/stats/modules/chart-utils.js`

**Step 1: 创建 chart-utils.js**
```javascript
// pages/stats/modules/chart-utils.js
// Canvas 2D 图表绘制工具

// 绘制折线图
function drawLineChart(ctx, config) {
  const { width, height, data, labels, padding = 40, lineColor = '#16744F', dotColor = '#16744F' } = config
  const W = width - padding * 2
  const H = height - padding * 2

  if (!data.length) return

  const maxVal = Math.max(...data, 1)
  const minVal = Math.min(0, ...data)

  // 坐标轴
  ctx.beginPath()
  ctx.strokeStyle = '#ddd'
  ctx.lineWidth = 1
  ctx.moveTo(padding, padding)
  ctx.lineTo(padding, height - padding)
  ctx.lineTo(width - padding, height - padding)
  ctx.stroke()

  // 数据线
  ctx.beginPath()
  ctx.strokeStyle = lineColor
  ctx.lineWidth = 2
  ctx.lineJoin = 'round'

  const points = data.map((v, i) => ({
    x: padding + (i / Math.max(data.length - 1, 1)) * W,
    y: height - padding - ((v - minVal) / Math.max(maxVal - minVal, 1)) * H
  }))

  points.forEach((p, i) => {
    if (i === 0) ctx.moveTo(p.x, p.y)
    else ctx.lineTo(p.x, p.y)
  })
  ctx.stroke()

  // 数据点
  points.forEach(p => {
    ctx.beginPath()
    ctx.fillStyle = dotColor
    ctx.arc(p.x, p.y, 3, 0, Math.PI * 2)
    ctx.fill()
  })

  return { points }
}

// 绘制雷达图
function drawRadar(ctx, config) {
  const { width, height, data, labels, maxVal = 100, color = 'rgba(22,116,79,0.3)', strokeColor = '#16744F' } = config
  const cx = width / 2, cy = height / 2
  const radius = Math.min(cx, cy) - 40
  const sides = data.length
  const angleStep = (Math.PI * 2) / sides

  // 网格
  for (let r = 1; r <= 4; r++) {
    ctx.beginPath()
    ctx.strokeStyle = '#e0e0e0'
    ctx.lineWidth = 0.5
    for (let i = 0; i < sides; i++) {
      const a = angleStep * i - Math.PI / 2
      const x = cx + Math.cos(a) * radius * (r / 4)
      const y = cy + Math.sin(a) * radius * (r / 4)
      if (i === 0) ctx.moveTo(x, y)
      else ctx.lineTo(x, y)
    }
    ctx.closePath()
    ctx.stroke()
  }

  // 轴线
  for (let i = 0; i < sides; i++) {
    ctx.beginPath()
    ctx.strokeStyle = '#e0e0e0'
    ctx.moveTo(cx, cy)
    const a = angleStep * i - Math.PI / 2
    ctx.lineTo(cx + Math.cos(a) * radius, cy + Math.sin(a) * radius)
    ctx.stroke()

    // 标签
    ctx.fillStyle = '#666'
    ctx.font = '11px sans-serif'
    ctx.textAlign = 'center'
    ctx.fillText(labels[i], cx + Math.cos(a) * (radius + 18), cy + Math.sin(a) * (radius + 18) + 4)
  }

  // 数据区
  ctx.beginPath()
  ctx.fillStyle = color
  ctx.strokeStyle = strokeColor
  ctx.lineWidth = 2
  for (let i = 0; i < sides; i++) {
    const a = angleStep * i - Math.PI / 2
    const r = radius * Math.min(data[i] / maxVal, 1)
    const x = cx + Math.cos(a) * r
    const y = cy + Math.sin(a) * r
    if (i === 0) ctx.moveTo(x, y)
    else ctx.lineTo(x, y)
  }
  ctx.closePath()
  ctx.fill()
  ctx.stroke()
}

// 绘制搭档网络图（简化版力导向）
function drawPartnerNetwork(ctx, config) {
  const { width, height, nodes, edges } = config
  // nodes: [{id, name, x, y, size, color}]
  // edges: [{from, to, weight}]

  ctx.clearRect(0, 0, width, height)

  // 连线
  edges.forEach(e => {
    const from = nodes.find(n => n.id === e.from)
    const to = nodes.find(n => n.id === e.to)
    if (!from || !to) return
    ctx.beginPath()
    ctx.strokeStyle = `rgba(22,116,79,${Math.min(e.weight / 20, 0.8)})`
    ctx.lineWidth = Math.max(e.weight / 5, 0.5)
    ctx.moveTo(from.x || 0, from.y || 0)
    ctx.lineTo(to.x || 0, to.y || 0)
    ctx.stroke()
  })

  // 节点
  nodes.forEach(n => {
    ctx.beginPath()
    ctx.fillStyle = n.color || '#16744F'
    ctx.arc(n.x || 0, n.y || 0, n.size || 12, 0, Math.PI * 2)
    ctx.fill()

    ctx.fillStyle = '#fff'
    ctx.font = `${Math.max((n.size || 12) * 0.5, 8)}px sans-serif`
    ctx.textAlign = 'center'
    ctx.textBaseline = 'middle'
    ctx.fillText(n.name || '', n.x || 0, n.y || 0)
  })
}

module.exports = { drawLineChart, drawRadar, drawPartnerNetwork }
```

### Task 2.2: 在 stats 分包添加数据看板页面

**Objective:** 增强现有 stats 页面，添加趋势图和搭档网络

**Files:**
- Modify: `pages/stats/index.js`
- Modify: `pages/stats/index.wxml`
- Modify: `pages/stats/index.wxss`

**Step 1: 修改 stats/index.wxml** — 添加 Canvas 图表区域

在现有统计内容后添加：
```xml
<!-- 胜率趋势图 -->
<view class="stats-section" wx:if="{{trendData.length}}">
  <text class="section-title">📈 胜率趋势</text>
  <canvas type="2d" id="trendCanvas" class="stats-canvas" style="width:{{canvasWidth}}px;height:220px"></canvas>
</view>

<!-- 搭档网络 -->
<view class="stats-section" wx:if="{{partnerNodes.length}}">
  <text class="section-title">🤝 搭档关系网络</text>
  <canvas type="2d" id="partnerCanvas" class="stats-canvas" style="width:{{canvasWidth}}px;height:300px"></canvas>
</view>
```

**Step 2: 修改 stats/index.js** — 添加图表绘制逻辑

在 onLoad/onShow 中添加：
```javascript
const { drawLineChart, drawPartnerNetwork } = require('./modules/chart-utils')

// 在数据处理后调用
drawTrendChart() {
  const query = wx.createSelectorQuery()
  query.select('#trendCanvas').fields({ node: true, size: true }).exec((res) => {
    if (!res[0]) return
    const canvas = res[0].node
    const ctx = canvas.getContext('2d')
    const dpr = wx.getSystemInfoSync().pixelRatio
    canvas.width = res[0].width * dpr
    canvas.height = res[0].height * dpr
    ctx.scale(dpr, dpr)

    const winRates = this.data.trendData.map(d => d.winRate)
    const labels = this.data.trendData.map(d => d.label)

    drawLineChart(ctx, {
      width: res[0].width,
      height: res[0].height,
      data: winRates,
      labels: labels,
      lineColor: '#16744F',
      dotColor: '#16744F'
    })
  })
},

drawPartnerNetworkChart() {
  const query = wx.createSelectorQuery()
  query.select('#partnerCanvas').fields({ node: true, size: true }).exec((res) => {
    if (!res[0]) return
    const canvas = res[0].node
    const ctx = canvas.getContext('2d')
    const dpr = wx.getSystemInfoSync().pixelRatio
    canvas.width = res[0].width * dpr
    canvas.height = res[0].height * dpr
    ctx.scale(dpr, dpr)

    drawPartnerNetwork(ctx, {
      width: res[0].width,
      height: res[0].height,
      nodes: this.data.partnerNodes,
      edges: this.data.partnerEdges
    })
  })
}
```

**Step 3: 修改 stats/index.wxss** — 添加 Canvas 样式
```css
.stats-canvas {
  width: 100%;
  height: 220px;
  margin: 16rpx 0;
  border-radius: 12rpx;
  background: var(--card-bg, #fff);
}

.stats-section {
  margin-top: 24rpx;
  padding: 16rpx;
}
```

### Task 2.3: 首页添加「数据看板」入口

**Objective:** 在首页添加跳转到 stats 分包的快捷入口

**Files:**
- Modify: `pages/index/index.wxml`

在首页 `home-hero` 区域添加快捷入口按钮组。

### Task 2.4: 数据聚合 — 从历史活动计算趋势

**Objective:** 在 stats/index.js 中实现趋势数据聚合

**Files:**
- Modify: `pages/stats/index.js`

在 stats onLoad 中添加从 `wx.getStorageSync('history')` 读取历史活动，计算每场的胜率、得分趋势，生成 `trendData`。

```javascript
computeTrends() {
  const history = wx.getStorageSync('history') || []
  const trendData = history.slice(-10).map((act, i) => {
    const games = act.games || act.completedSchedule || []
    let wins = 0, total = 0
    games.forEach(g => {
      if (g.myTeam === 'A' && g.scoreA > g.scoreB) wins++
      if (g.myTeam === 'B' && g.scoreB > g.scoreA) wins++
      if (g.myTeam) total++
    })
    return {
      label: (act.date || '').slice(5), // MM-DD
      winRate: total ? Math.round(wins / total * 100) : 0,
      games: total
    }
  })
  this.setData({ trendData })
}
```

### Task 2.5: 验证并提交

```bash
cd /c/Users/Lenovo/Desktop/forBadminton/forBadminton
node --check pages/stats/modules/chart-utils.js && echo "CHART SYNTAX OK"
node --check pages/stats/index.js && echo "STATS SYNTAX OK"
git add -A && git commit -m "feat(dashboard): 数据看板 — Canvas趋势图+搭档网络+入口"
```

---

## 功能三：🎨 首页重构

### 背景
当前首页是简单的列表布局。需要改为：
1. 顶部 Hero 区：今日活动大卡片 + 倒计时
2. 快捷操作：发起活动 / 扫码签到 / 查看统计
3. 球友动态滚动
4. 视觉升级：毛玻璃卡片、运动感渐变

### Task 3.1: 首页 WXML 重构

**Objective:** 重写首页区域为卡片式布局

**Files:**
- Modify: `pages/index/index.wxml` (替换 `home` tab 区域)

**完成代码:**

替换 `<view wx:if="{{mainTab === 'home'}}">` 整块（L36 起）：

```xml
  <view wx:if="{{mainTab === 'home'}}" class="home-v2">
    <!-- Hero 区 -->
    <view class="hero-banner">
      <view class="hero-bg"></view>
      <view class="hero-content">
        <text class="hero-title">🏸 羽毛球打转</text>
        <text class="hero-subtitle">智能轮转 · 数据复盘 · 球友社区</text>
      </view>
      <view class="hero-stats">
        <view class="hero-stat-item">
          <text class="hero-stat-num">{{recentActivities.length}}</text>
          <text class="hero-stat-label">场活动</text>
        </view>
        <view class="hero-stat-item">
          <text class="hero-stat-num">{{profileStats.matches}}</text>
          <text class="hero-stat-label">次出战</text>
        </view>
        <view class="hero-stat-item">
          <text class="hero-stat-num">{{profileStats.winRate}}</text>
          <text class="hero-stat-label">胜率</text>
        </view>
      </view>
    </view>

    <!-- 快捷操作 -->
    <view class="quick-actions">
      <button class="qa-btn qa-primary" bindtap="startNewActivity">
        <text class="qa-icon">➕</text>
        <text class="qa-label">发起活动</text>
      </button>
      <button class="qa-btn" bindtap="scanQrCode">
        <text class="qa-icon">📷</text>
        <text class="qa-label">扫码签到</text>
      </button>
      <button class="qa-btn" bindtap="openStats">
        <text class="qa-icon">📊</text>
        <text class="qa-label">数据看板</text>
      </button>
      <button class="qa-btn" bindtap="openHistory">
        <text class="qa-icon">📋</text>
        <text class="qa-label">活动历史</text>
      </button>
    </view>

    <!-- 当前进行中的活动大卡片 -->
    <view class="section" wx:if="{{activeActivities.length}}">
      <view class="section-header">
        <text class="section-title">🔥 进行中</text>
        <text class="section-count">{{activeActivities.length}} 场</text>
      </view>
      <scroll-view class="active-cards-h" scroll-x>
        <view class="active-card-featured" wx:for="{{activeActivities}}" wx:key="id" data-id="{{item.id}}" bindtap="openActivityCard">
          <view class="afc-header">
            <text class="afc-title">{{item.title}}</text>
            <view class="afc-badge {{item.status}}">{{item.statusText}}</view>
          </view>
          <view class="afc-body">
            <view class="afc-info-row">
              <text>📅 {{item.date}}</text>
              <text>📍 {{item.venue}}</text>
            </view>
            <view class="afc-info-row">
              <text>👥 {{item.playerCount}} 人</text>
              <text>🏸 {{item.courtCount}} 片场</text>
              <text>🎯 {{item.gameCount}} 局</text>
            </view>
            <view class="afc-progress">
              <text class="afc-progress-text">{{item.completed || 0}}/{{item.gameCount}} 计分</text>
              <view class="afc-progress-bar">
                <view class="afc-progress-fill" style="width:{{(item.completed || 0) / (item.gameCount || 1) * 100}}%"></view>
              </view>
            </view>
          </view>
          <view class="afc-footer">
            <text class="afc-updated">更新 {{item.updatedText || '-'}}</text>
            <text class="afc-arrow">→</text>
          </view>
        </view>
      </scroll-view>
    </view>

    <!-- 搜索 + 已结束活动列表 -->
    <view class="section">
      <view class="section-header">
        <text class="section-title">📦 已结束</text>
        <view class="home-search-wrap">
          <input class="home-search-inline" value="{{homeKeyword}}" bindinput="onHomeKeywordInput" placeholder="🔍 搜索" />
        </view>
      </view>
      <scroll-view wx:if="{{endedActivities.length}}" class="activity-list" scroll-y>
        <view class="ended-card" wx:for="{{endedActivities}}" wx:key="id" data-id="{{item.id}}" bindtap="openActivityCard">
          <view class="ec-left">
            <text class="ec-title">{{item.title}}</text>
            <text class="ec-meta">{{item.date}} · {{item.venue}} · {{item.playerCount}}人 · {{item.courtCount}}场</text>
          </view>
          <view class="ec-right">
            <text class="ec-arrow">→</text>
          </view>
        </view>
      </scroll-view>
      <view wx:if="{{!endedActivities.length}}" class="empty-state">暂无已结束活动。</view>
      <button wx:if="{{endedHasMore}}" class="load-more" data-section="ended" bindtap="loadMoreHomeSection">加载更多</button>
    </view>
  </view>
```

### Task 3.2: 首页样式 — Hero + 卡片主题

**Objective:** 运动感视觉语言，毛玻璃卡片

**Files:**
- Modify: `pages/index/styles/theme.wxss` (追加样式)
- Or create: `pages/index/styles/home-v2.wxss` + @import

**完成代码:**

在 theme.wxss 末尾追加（或在 index.wxss 中 @import 新文件）：

```css
/* ============ Home V2 Styles ============ */
.home-v2 {
  padding-bottom: 40rpx;
}

/* Hero Banner */
.hero-banner {
  position: relative;
  margin: 16rpx 24rpx;
  padding: 40rpx 32rpx;
  border-radius: 24rpx;
  overflow: hidden;
  background: linear-gradient(135deg, #16744F 0%, #1a8a5a 40%, #0d4a30 100%);
  box-shadow: 0 8rpx 32rpx rgba(22, 116, 79, 0.3);
}

.hero-bg {
  position: absolute;
  top: -50%;
  right: -20%;
  width: 300rpx;
  height: 300rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.08);
}

.hero-bg::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 200rpx;
  height: 200rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.05);
  transform: translate(-30%, -30%);
}

.hero-content {
  position: relative;
  z-index: 1;
}

.hero-title {
  display: block;
  font-size: 40rpx;
  font-weight: 700;
  color: #fff;
  margin-bottom: 8rpx;
}

.hero-subtitle {
  display: block;
  font-size: 24rpx;
  color: rgba(255,255,255,0.75);
}

.hero-stats {
  position: relative;
  z-index: 1;
  display: flex;
  margin-top: 28rpx;
  gap: 0;
  background: rgba(255,255,255,0.15);
  border-radius: 16rpx;
  padding: 16rpx 0;
  backdrop-filter: blur(10px);
}

.hero-stat-item {
  flex: 1;
  text-align: center;
  border-right: 1rpx solid rgba(255,255,255,0.2);
}

.hero-stat-item:last-child {
  border-right: none;
}

.hero-stat-num {
  display: block;
  font-size: 36rpx;
  font-weight: 700;
  color: #fff;
}

.hero-stat-label {
  display: block;
  font-size: 20rpx;
  color: rgba(255,255,255,0.7);
  margin-top: 4rpx;
}

/* Quick Actions */
.quick-actions {
  display: flex;
  gap: 16rpx;
  padding: 24rpx;
  margin: -24rpx 24rpx 16rpx;
  background: var(--card-bg, #fff);
  border-radius: 20rpx;
  box-shadow: 0 2rpx 16rpx rgba(0,0,0,0.06);
}

.qa-btn {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  padding: 20rpx 0;
  background: transparent;
  border: none;
  border-radius: 16rpx;
  font-size: 22rpx;
  line-height: 1;
}

.qa-btn::after {
  border: none;
}

.qa-primary {
  background: linear-gradient(135deg, #16744F, #1a8a5a);
}

.qa-primary .qa-label {
  color: #fff;
}

.qa-icon {
  font-size: 36rpx;
  display: block;
}

.qa-label {
  display: block;
  color: var(--text-color, #333);
  font-size: 22rpx;
}

/* Section Header */
.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8rpx 0;
}

.section-count {
  font-size: 24rpx;
  color: #999;
}

.home-search-wrap {
  width: 280rpx;
}

.home-search-inline {
  height: 56rpx;
  padding: 0 24rpx;
  background: var(--input-bg, #f5f5f5);
  border-radius: 28rpx;
  font-size: 24rpx;
}

/* Featured Active Cards (Horizontal Scroll) */
.active-cards-h {
  display: flex;
  white-space: nowrap;
  padding: 8rpx 0;
}

.active-card-featured {
  display: inline-block;
  width: 560rpx;
  margin-right: 20rpx;
  padding: 24rpx;
  background: var(--card-bg, #fff);
  border-radius: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.08);
  white-space: normal;
  transition: transform 0.2s;
}

.active-card-featured:active {
  transform: scale(0.97);
}

.afc-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 16rpx;
}

.afc-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text-color, #333);
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
}

.afc-badge {
  font-size: 20rpx;
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
  white-space: nowrap;
  margin-left: 12rpx;
}

.afc-badge.signup { background: #e8f5e9; color: #16744F; }
.afc-badge.locked { background: #fff3e0; color: #e65100; }
.afc-badge.started { background: #e3f2fd; color: #1565c0; }

.afc-body {
  margin-bottom: 16rpx;
}

.afc-info-row {
  display: flex;
  gap: 24rpx;
  font-size: 24rpx;
  color: var(--text-secondary, #666);
  margin-bottom: 8rpx;
}

.afc-progress {
  margin-top: 12rpx;
}

.afc-progress-text {
  font-size: 22rpx;
  color: #999;
}

.afc-progress-bar {
  height: 6rpx;
  background: #eee;
  border-radius: 3rpx;
  margin-top: 8rpx;
  overflow: hidden;
}

.afc-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #16744F, #1a8a5a);
  border-radius: 3rpx;
  transition: width 0.3s;
}

.afc-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 16rpx;
  border-top: 1rpx solid var(--border-color, #f0f0f0);
}

.afc-updated {
  font-size: 22rpx;
  color: #bbb;
}

.afc-arrow {
  font-size: 28rpx;
  color: #16744F;
}

/* Ended Cards (Compact) */
.ended-card {
  display: flex;
  align-items: center;
  padding: 20rpx 24rpx;
  margin-bottom: 12rpx;
  background: var(--card-bg, #fff);
  border-radius: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}

.ec-left {
  flex: 1;
  min-width: 0;
}

.ec-title {
  display: block;
  font-size: 28rpx;
  font-weight: 500;
  color: var(--text-color, #333);
  margin-bottom: 4rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.ec-meta {
  display: block;
  font-size: 22rpx;
  color: #999;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.ec-right .ec-arrow {
  font-size: 32rpx;
  color: #ccc;
  padding-left: 16rpx;
}
```

### Task 3.3: 添加快捷操作 JS 方法

**Objective:** 实现快捷按钮的跳转逻辑

**Files:**
- Modify: `pages/index/index.js`

在 Page 对象的方法中添加：
```javascript
scanQrCode() {
  wx.scanCode({
    success: (res) => {
      if (res.result && res.result.includes('activityId=')) {
        const match = res.result.match(/activityId=([^&\s]+)/)
        if (match) {
          this.loadActivity(match[1], 'player')
        }
      } else {
        wx.showToast({ title: '无效的活动码', icon: 'none' })
      }
    },
    fail: () => {
      wx.showToast({ title: '扫码取消', icon: 'none' })
    }
  })
},

openStats() {
  wx.navigateTo({ url: '/pages/stats/index' })
},

openHistory() {
  wx.navigateTo({ url: '/pages/history/index' })
}
```

### Task 3.4: 暗色模式适配

**Objective:** 首页新组件支持暗色模式

**Files:**
- Modify: `pages/index/styles/theme.wxss`

在新样式中使用 CSS 变量：
```css
[data-theme="dark"] .active-card-featured,
[data-theme="dark"] .ended-card,
[data-theme="dark"] .quick-actions {
  background: #1e1e1e;
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.3);
}

[data-theme="dark"] .afc-title,
[data-theme="dark"] .ec-title {
  color: #e0e0e0;
}

[data-theme="dark"] .afc-info-row,
[data-theme="dark"] .ec-meta {
  color: #999;
}

[data-theme="dark"] .afc-footer {
  border-top-color: #333;
}

[data-theme="dark"] .home-search-inline {
  background: #2a2a2a;
  color: #e0e0e0;
}

[data-theme="dark"] .afc-progress-bar {
  background: #333;
}
```

### Task 3.5: 验证并提交

```bash
cd /c/Users/Lenovo/Desktop/forBadminton/forBadminton
node --check pages/index/index.js && echo "INDEX SYNTAX OK"
git add -A && git commit -m "feat(home): 首页重构 — Hero卡片+快捷操作+暗色适配"
```

---

## 执行顺序建议

由于三个特性文件交集主要在 `index.js` 和 `index.wxml`，建议按以下顺序以避免冲突：

1. **Task 1.1-1.3** 先做：消息通知的基础设施（独立文件）
2. **Task 2.1** 先做：图表工具模块（独立文件）
3. **Task 3.1-3.4** 先做：首页重构（改动 WXML/样式大，先占位）
4. **Task 1.4-1.5** 后做：消息通知集成（依赖 index.js）
5. **Task 2.2-2.4** 后做：数据看板集成（依赖 stats 分包）

## 总提交数

| 阶段 | Commit | 内容 |
|------|--------|------|
| 1 | `feat(notify): 权限声明+云函数+模块` | Task 1.1 + 1.2 + 1.3 |
| 2 | `feat(dashboard): Canvas 图表绘制工具` | Task 2.1 |
| 3 | `feat(home): 首页重构 v2 — Hero+卡片+快捷操作` | Task 3.1-3.4 |
| 4 | `feat(notify): 订阅授权提示+事件触发` | Task 1.4 + 1.5 |
| 5 | `feat(dashboard): 数据看板集成 — 趋势图+搭档网络` | Task 2.2-2.4 |
| 6 | `feat: v2.1 全量优化 — 通知+看板+首页` | 合并确认 |

---

## 风险点

1. **index.js 单文件过大** (~3900行→4500+): 后续考虑拆分为 Behavior 或独立模块
2. **订阅消息模板 ID 为空**: 需老大在微信公众平台配置后才能生效，代码逻辑已就绪
3. **Canvas 2d 兼容性**: 基础库 2.9.0+ 支持，当前 `libVersion: 3.16.0` 没问题
4. **暗色模式变量**: 依赖现有 `[data-theme="dark"]` 机制，需确保变量名一致
